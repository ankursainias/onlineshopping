<div class="page-header">
  <h1>Listing Toppings</h1>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th>Active</th>
    </tr>
  </thead>

  <tbody>
    <% @toppings.each do |topping| %>
      <tr>
        <td><%= topping.name %></td>
        <td><%= number_to_currency(topping.price, :unit => "£") %></td>
        <td><%= topping.active ? "Yes" : "No" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Topping', new_admin_topping_path, class: 'btn btn-primary' %>
