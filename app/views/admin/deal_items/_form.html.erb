<%= form_for([:admin, @deal_item]) do |f| %>
  <% if @deal_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deal_item.errors.count, "error") %> prohibited this deal_item from being saved:</h2>

      <ul>
      <% @deal_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :deal_id %><br>
    <%= f.text_field :deal_id %>
    </div>
  <div class="field">
      <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
